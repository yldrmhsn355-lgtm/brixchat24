# Meta WhatsApp setup

Use a production Meta app, verified business and dedicated phone number. Store app secret, access token, phone-number ID and verify token in the secret manager. Register the HTTPS webhook, verify signatures, then run all `acceptance:meta:*` scenarios with consented test recipients. Preserve message IDs and delivery evidence without recording tokens or message content.
